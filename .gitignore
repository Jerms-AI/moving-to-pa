# OS / editor
.DS_Store
Thumbs.db
*.swp
.vscode/
.idea/

# Node (if tooling gets added later)
node_modules/
npm-debug.log*

# Scratch
*.tmp
.cache/

# Realtor email source files (personal; kept local)
*.eml
*.htm
email*.html

# Python
__pycache__/
*.pyc

# PA assessment source spreadsheets (large, local)
.assessment_src/

# Personal listing notes (local, written by serve.py / notes.php)
notes.json
notes_data.json

# RealScout sync — credentials, saved session, scratch dumps (all local/personal)
.realscout_creds.json
.realscout_state.json
realscout_raw.json
realscout_api.json
realscout_dump.html
realscout_dump*.png
realscout_sync.log

# Phone-access helper (local Windows port-forward script)
_phone_setup.cmd

# Zillow sync — exported session cookies + scratch dumps (all local/personal)
.zillow_cookies.json
zillow_raw.json
zillow_api.json
zillow_dump.html
zillow_dump*.png
